#include "main.h"

/**
 * _isdigit - Checks for a digit
 * @c: takes a digit to be cheked
 * Return: 1 if a digit if not 0
 */
int _isdigit(int c)
{
	if (c >= '0' && c <= '0')
		return (1);
	else
		return (0);
}
